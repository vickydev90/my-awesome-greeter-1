application: test123
distribution:
    dir: dist-release
nexus:
  url: dev-p76-app-16.sandbox.local:8081/nexus
  credentials: nexusLocal
environments:
     targetEnv: qa
     targetbranch: 
prefix: /usr/local/bin

